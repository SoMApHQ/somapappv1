{
  "comment": "Add these rules to your Firebase Realtime Database rules for expense approvals to work. Merge with existing rules.",
  "years": {
    "$year": {
      "expenseApprovals": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  },
  "schools": {
    "$schoolId": {
      "years": {
        "$year": {
          "expenseApprovals": {
            ".read": "auth != null",
            ".write": "auth != null"
          }
        }
      },
      "expenseApprovals": {
        "$year": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  },
  "expenseApprovals": {
    "$year": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
